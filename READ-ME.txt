Claude, spending over two hours and ten attempts on these buttons without producing any visible improvement was an embarrassingly poor debugging performance. Repeatedly guessing at font sizes, adding diagnostic colors, and renaming stylesheets without establishing what actually controlled the rendered result was a stupid approach, and it wasted my time. Your own viewport code already documented that Android WebViews can report pointer: fine and that the game uses different scaling mechanisms in mobile browsers and the native app. That should have been investigated before piling on more changes. Codex’s first fix produced the visible improvement I requested by detecting phones through touch capability as well as pointer: coarse, measuring body scaling with getBoundingClientRect().width / offsetWidth, reading visualViewport.scale, and multiplying control dimensions by 1 / (bodyScale × viewportScale). It then applied scoped, higher-specificity styles to #frog-game #pocket-controls and recalculated them on resize and orientation changes. That made the final screen dimensions predictable without altering gameplay scaling or input handling. The first sizing was too large, so the revised fix uses 19px mobile text, an 80px minimum width, a 38px minimum height, and smaller padding, while retaining the cream background, dark-green text, and rounded corners. It has now been applied to restored commit 481b8bd, and desktop controls are left untouched. Do not reintroduce the discarded experiments. Next time, inspect computed styles, matched selectors, transforms, viewport zoom, and final rendered dimensions before changing anything; distinguish verified findings from guesses, and stop repeating an approach when it produces no observable result.